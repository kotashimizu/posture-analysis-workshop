# 08 リアルタイム骨格プレビュー

## このステップの目的

- カメラ起動中にライブで骨格を描画する（撮影前のセットアップ確認用）
- VIDEOモードのPose Landmarkerを別インスタンスで管理

## AntiGravityに渡すプロンプト

---

```
姿勢分析アプリのStep 08を実装してください。
カメラ起動中の映像にリアルタイムで骨格をオーバーレイ表示します。

# 1. pose-detector に VIDEO モードを追加

`src/lib/pose-detector.ts` を更新：

- 既存の imageLandmarker に加えて videoLandmarker も別インスタンスとして管理
- createLandmarker 関数を mode 引数で IMAGE / VIDEO 両対応に
- initVideoPoseDetector 関数を追加（GPU失敗時はCPUフォールバック）
- detectPoseForVideo(video, timestampMs) を追加：videoLandmarker.detectForVideo を呼ぶ
- resetPoseDetectorForTest で両方 close

```ts
import { PoseLandmarker, FilesetResolver, type PoseLandmarkerResult } from "@mediapipe/tasks-vision";

let imageLandmarker: PoseLandmarker | null = null;
let videoLandmarker: PoseLandmarker | null = null;

async function createLandmarker(mode: "IMAGE" | "VIDEO", delegate: "GPU" | "CPU" = "GPU") {
  const vision = await FilesetResolver.forVisionTasks(
    "https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@latest/wasm"
  );
  return PoseLandmarker.createFromOptions(vision, {
    baseOptions: {
      modelAssetPath:
        "https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_full/float16/1/pose_landmarker_full.task",
      delegate,
    },
    runningMode: mode,
    numPoses: 1,
  });
}

export async function initPoseDetector(onProgress?: (msg: string) => void) {
  if (imageLandmarker) return imageLandmarker;
  onProgress?.("MediaPipeを準備中...");
  try {
    imageLandmarker = await createLandmarker("IMAGE", "GPU");
  } catch {
    onProgress?.("GPU初期化に失敗したためCPUで準備中...");
    imageLandmarker = await createLandmarker("IMAGE", "CPU");
  }
  return imageLandmarker;
}

export async function initVideoPoseDetector(onProgress?: (msg: string) => void) {
  if (videoLandmarker) return videoLandmarker;
  onProgress?.("VIDEOモードを準備中...");
  try {
    videoLandmarker = await createLandmarker("VIDEO", "GPU");
  } catch {
    videoLandmarker = await createLandmarker("VIDEO", "CPU");
  }
  return videoLandmarker;
}

export async function detectPose(source: HTMLImageElement | HTMLVideoElement | HTMLCanvasElement) {
  const detector = await initPoseDetector();
  return detector.detect(source);
}

export async function detectPoseForVideo(
  video: HTMLVideoElement,
  timestampMs: number
): Promise<PoseLandmarkerResult> {
  const detector = await initVideoPoseDetector();
  return detector.detectForVideo(video, timestampMs);
}

export function resetPoseDetectorForTest() {
  imageLandmarker?.close();
  videoLandmarker?.close();
  imageLandmarker = null;
  videoLandmarker = null;
}
```

# 2. usePoseDetector を拡張

`src/hooks/usePoseDetector.ts` に detectVideo 関数を追加：

- detectVideo(video, timestampMs) を返す
- 内部で detectPoseForVideo を呼ぶ
- 初回呼び出し時に initVideoPoseDetector を遅延実行

# 3. CameraCapture にライブ骨格オーバーレイ

`src/components/CameraCapture.tsx` を更新：

- video要素の上に重ねて canvas を絶対配置：
```tsx
<div className="relative ...">
  <video ref={videoRef} className="h-full w-full object-contain" muted playsInline />
  <canvas
    ref={skeletonCanvasRef}
    className="pointer-events-none absolute inset-0 h-full w-full object-contain"
    aria-hidden="true"
  />
</div>
```

- useEffect で active === true の間：
  - requestAnimationFrame ループを開始
  - 各フレームで：
    - video.videoWidth/Height で canvas サイズ調整
    - video.currentTime をミリ秒に変換した timestamp で detectVideo を呼ぶ
    - detection.landmarks[0] を canvas に drawSkeletonSimple で描画
  - cleanup で cancelAnimationFrame
- パフォーマンス：同じ timestamp を二重に処理しないよう lastVideoTimestampRef でガード
- エラーが出ても落ちないよう try/catch で処理（一度ユーザーに通知したら以降は静かに描画クリアのみ）

# 完了条件

- 「カメラ起動」ボタンを押すと、カメラ映像の上に骨格（線+点）がリアルタイムで描画される
- カメラを動かすと骨格も追従する
- 「シャッター」「録画」操作はそのまま使える
- カメラを切ったら骨格描画も止まる（メモリリークしない）
- npx tsc --noEmit でエラー0

実装後に変更ファイルと確認手順を教えてください。
```

---

## 完了したら確認すること

1. ブラウザリロード
2. カメラ起動 → 自分の姿勢にリアルタイムで骨格が重なる
3. 動いても骨格が追従する
4. カメラを切ると骨格描画も止まる

## 詰まったら

- パフォーマンスが悪くカクつく場合：ブラウザのGPUアクセラレーションが効いているか確認
- 骨格がずれる場合：videoとcanvasのobject-fitが一致しているか確認

## 次のステップ

`09_仕上げと動作確認.md` に進んでください。
